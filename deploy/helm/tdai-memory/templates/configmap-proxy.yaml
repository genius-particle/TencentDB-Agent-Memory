apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tdai-memory.fullname" . }}-proxy-config
  labels:
    {{- include "tdai-memory.labels" . | nindent 4 }}
data:
  config.yaml.tmpl: |
    {{- include "tdai-memory.proxyYamlTmpl" . | nindent 4 }}
